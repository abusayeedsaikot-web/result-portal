# Result Portal PDF Parser V3

V3 keeps the page-by-page PDF.js parser and optimizes database saving with a single SQLite transaction. It also prevents a Roll search from being sent while PDF import is running.

Run:
npm install
$env:ADMIN_KEY="BTEB@Result#Admin2026!X9pL7"
npm start

Open http://localhost:3000
